experiment:
  summary: binary classification - feature  and model selection
  classification_type: binary
  phase: feature_model_selection # feature and model selection
  tag: final
  verbosity: 0
  random_seed: 42

data:
  dataset_path: ../dataset/Sudoscan Working File with Stats.xlsx

feature_selection:
  cross_validation:
    k_splits: 4 
    n_repeats: 10
    scoring : youden
  vif_threshold: 5
  figures:
    youden_table_topk: 5