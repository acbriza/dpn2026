experiment:
  summary: binary classification - feature  and model selection (final experiment) after code review using dataset for public request
  classification_type: binary
  stage: selection # feature and model selection
  tag: final_202608
  verbosity: 0
  random_seed: 42

data:
  dataset_path: ../dataset/EAMC_DPN_Dataset.xlsx

feature_selection:
  cross_validation:
    k_splits: 4
    n_repeats: 10
    sort_by: auprc
  vif_threshold: 5

figures:
  summary_table_topk: 5